import React from "react";

function Header(){
    return(
        <div><h1>Daily Journal</h1>
        <button>Home</button>
        <button>Compose</button>
        <button>About us</button>
        <button>Contact us</button>
        </div>
    );
}

export default Header;